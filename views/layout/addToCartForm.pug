mixin addToCartForm(id)
 form(action="/cart" method="POST")
    button.btn(type="submit") Add to Cart    
    input(type="hidden", name="_csrf" value=csrfToken)
    input(type="hidden" name="prodId", value=id)